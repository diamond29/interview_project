require_relative 'spec_helper.rb'
require 'interview_project'

# this might not really need a test if the components are tested
# It relies on the service actually running, which is a little fancier than
# I think the test needs to be at the moment.
describe 'Client Script' do
  # it 'should run without crashing' do
  #   load File.expand_path '../../bin/client', __FILE__
  # end
end
